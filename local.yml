---
- hosts: localhost
  connection: local
  become: true

  pre_tasks:
    - name: update repos
      pacman:
        update_cache: yes
        upgrade: yes
      changed_when: False

  tasks:
    - include: tasks/packages.yml
    - include: tasks/autofs.yml